var $ff = false;

//port object crossbrowser compatable

var xBport = function(portName,portDesc=portName){
	portName = chrome.extension.connect({name: portdesc}); 
	this.addListener(callback) = portname.onMessage.addListener(callback);
	this.postMessage(msgObj) = portname.postMessage(msgObj);
};
var 
$ff?

//everything is wrapped in JQuery 
$(document).ready(function(){
 //sets ff var 
 if(!$.browser.webkit){
	$ff = true;
 }



});
